[project]
name = "training-wheels"
version = "0.1.0"
description = "Training module for TherapML Team"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "einops>=0.8.2",
    "huggingface-hub[cli]>=1.7.1",
    "jaxtyping>=0.3.7",
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
    "pybind11>=3.0.1",
    "pytest>=9.0.2",
    "scipy>=1.17.0",
    "setuptools>=82.0.0",
    "tokenizers>=0.22.1",
    "torch>=2.10.0",
    "tqdm>=4.67.3",
]

[build-system]
requires = ["uv_build", "setuptools", "pybind11"]
build-backend = "setuptools.build_meta"

[tool.uv.build-backend]
module-name = "therapml"
module-root = ""

[tool.python.ini_options]
log_cli = true
log_cli_level = "INFO"
addopts = "-s"

[tool.ruff]
line-length = 120

[tool.ruff.lint.extend-per-file-ignores]
# Also ignore `E402` in all `__init__.py` files.
"__init__.py" = ["E402", "F401", "F403", "E501"]

[tool.ruff.lint]
extend-select = ["UP"]
ignore = [
    "F722"
]
